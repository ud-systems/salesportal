# Netlify configuration — Vite + React (SPA)
# https://docs.netlify.com/configure-builds/file-based-configuration/
# Development ownership: Ian Katana

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Same-origin Supabase proxy — browser only sees /api/db/*, not *.supabase.co in DevTools.
[[redirects]]
  from = "/api/db/*"
  to = "https://xbmpndatdanjewhwxzxr.supabase.co/:splat"
  status = 200
  force = true

# Client-side routing: serve index.html for all non-file paths so React Router
# handles /dashboard, /login, deep links, and refreshes on nested routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
